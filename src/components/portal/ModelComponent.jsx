const ModelComponent = ({ closeHandler }) => {
  return (
    <div className="modal">
      <div>Hello shiva</div>
      <button onClick={closeHandler}>Close</button>
    </div>
  );
};
export default ModelComponent;
